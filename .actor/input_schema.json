{
  "title": "Zameen.com Property Scraper",
  "description": "Scrape property listings from Zameen.com canonical listing pages (recommended). If you don’t have a listing URL, provide `location + keyword` and the actor will resolve a canonical listing URL (avoids /search which often returns 503).",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "Array of Zameen.com LISTING URLs (recommended). Use canonical listing pages like https://www.zameen.com/Homes/Lahore-1-1.html or https://www.zameen.com/Homes/Lahore_Bahria_Town-509-1.html (avoid /search which often returns 503).",
      "editor": "stringList",
      "prefill": ["https://www.zameen.com/Homes/Pakistan-1-1.html"]
    },
    "category": {
      "title": "Category",
      "type": "string",
      "description": "Which Zameen category to resolve when using keyword+location (ignored when using startUrls).",
      "editor": "select",
      "enum": ["Homes", "Plots", "Commercial"],
      "default": "Homes"
    },
    "keyword": {
      "title": "Keyword",
      "type": "string",
      "description": "Area/development keyword to resolve within the city (e.g., 'Bahria Town', 'DHA', 'G-13').",
      "editor": "textfield"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "City name (e.g., 'Lahore', 'Islamabad', 'Karachi'). Used together with keyword to build a canonical listing URL.",
      "editor": "textfield"
    },
    "results_wanted": {
      "title": "Maximum listings",
      "type": "integer",
      "description": "Maximum number of listings to collect across all pages.",
      "minimum": 1,
      "default": 100,
      "editor": "number"
    },
    "max_pages": {
      "title": "Pagination limit",
      "type": "integer",
      "description": "Safety cap on how many listing pages to open per start URL.",
      "minimum": 1,
      "default": 20,
      "editor": "number"
    },
    "scrapeDetails": {
      "title": "Open detail pages",
      "type": "boolean",
      "description": "If enabled, actor opens each listing detail page for full data (recommended).",
      "default": true,
      "editor": "checkbox"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Use Apify Proxy to reduce blocking. Data center works; residential is optional for heavy runs.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
